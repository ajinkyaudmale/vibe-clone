import { useCursorTracker } from '@/hooks/useCursorTracker';

export function LessonCard() {
  useCursorTracker();
  
  return (
    <div className="dynamic-shadow bg-white p-6 rounded-xl relative">
      {/* Lesson content */}
    </div>
  );
}
